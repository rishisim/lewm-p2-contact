dataset:
  name: cube_single_expert.lance
  num_steps: ${eval:'${num_preds} + ${history_size}'}
  frameskip: 5
  keys_to_load:
    - pixels
    - action
    - observation
    - proprio
  keys_to_cache:
    - action
    - observation
    - proprio
