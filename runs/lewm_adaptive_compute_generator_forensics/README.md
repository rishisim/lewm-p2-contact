# LeWM Cube generator forensics

Append-only artifacts for the preregistered offline-to-local reconstruction audit.
Prior runs are immutable inputs. See `PREREGISTRATION.md`, `protocol.json`,
`REPORT.md`, `metrics.json`, `decision.json`, and `provenance/provenance.json`.

The audit used exactly 8 isolated discovery episodes, 64 one-step probes, and 24
frames. It generated no policy trajectories and performed no model or gate work.
