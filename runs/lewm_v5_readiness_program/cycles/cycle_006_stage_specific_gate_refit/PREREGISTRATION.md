# LeWM V5-readiness cycle 006 preregistration

This is a prospective discovery cycle, not V5 confirmation. It follows the preserved cycle-005 negative result and changes only the gate parameterization: each of the three decisions receives its own dual affine head over the same 1,046 causal, counted, contact-free features. The base model, refiner, native target whitening, PlanOracle DGP, exact-compute comparators, bootstrap, simultaneous inference, sparse execution, numerical contract, and every success criterion remain fixed.

Exactly 240 fresh fit episodes and 120 disjoint fresh selection episodes train and choose among the nine candidates fixed in `candidate_grid.json`; selection never refits a head. The selected gate must pass fit/selection isolation, CPU/MPS call determinism, exact operation accounting, repository tests, and a gate-freeze seal. Then exactly 12 new excluded smoke episodes must pass. Only then are exactly 300 wholly fresh prospective episodes generated, with no replacement except a preregistered mechanical generation exception, no sequential expansion, and no criterion change.

The actual adaptive output is produced by the manual sparse stage loop. Dense execution is only a separately accounted comparator and numerical shadow. All generation and execution traces are complete and hashed before prospective target analysis. The outcome follows `outcome_mapping.json`, and a second read-only implementation must reproduce all effects, intervals, calls, compute, ranks, path hashes, and mapping before a discovery pass can be accepted. No V5 confirmation episode may be generated.
