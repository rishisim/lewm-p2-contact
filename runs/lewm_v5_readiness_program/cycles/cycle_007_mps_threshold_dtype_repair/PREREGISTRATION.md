# LeWM V5-readiness cycle 007 preregistration

This is a prospective discovery cycle, not V5 confirmation. Cycle 006 validly fit and selected one stage-specific dual-linear gate on isolated fresh development roles, then became implementation-invalid before smoke because its sealed MPS runner attempted to construct an unsupported float64 threshold tensor. Cycle 007 carries the selected candidate and every numeric gate artifact forward byte-for-byte. It performs no new fit, no reselection, and no head refit.

The sole executable repair is fixed before new data: the three stored float64 threshold values are converted to float32 when placed on device, matching the compiled affine heads and gate scores. Before the pre-data seal, the repaired implementation must prove exact equality of calls between the stored float64 thresholds and runtime float32 thresholds on all 9,120 consumed fit rows and all 4,560 consumed selection rows. It must also pass the complete CPU and MPS sparse/dense runtime qualification. Any failure invalidates this cycle; no normative in-place patch is allowed.

The base model, refiner, compiled gate coefficients, numeric thresholds, native target whitening, 1,046-feature causal graph, 7,985-FLOP gate count, PlanOracle DGP, exact-compute comparators, 20,000-replicate episode bootstrap, Bonferroni co-primary inference, sparse numerical contract, and every success criterion remain fixed. Exactly 12 new disjoint excluded smoke episodes must pass. Only then are exactly 300 wholly fresh prospective episodes generated, with no sequential expansion and no outcome-based replacement.

The actual adaptive output is produced by the manual sparse stage loop. Dense execution is only a separately accounted comparator and numerical shadow. The input loader allowlist is exactly `action,pixels`; contact, privileged fields, V3 test targets, the combined V3 cache, and the released HDF5 corpus are forbidden. All generation and execution traces are complete and hashed before prospective target analysis. The immutable outcome follows `outcome_mapping.json`, and a second read-only implementation must reproduce all effects, intervals, calls, compute, ranks, hashes, and terminal mapping before a pass can be accepted. No V5 confirmation episode may be generated.
